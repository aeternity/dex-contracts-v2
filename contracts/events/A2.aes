@compiler >= 6

include "./IB.aes"
contract A2 =
    entrypoint init() = ()

    //This will cause an error in sdk when methodA is been called
    datatype event = SomeEvent(address)

    entrypoint methodA2(b: IB) =
        b.methodB()
